# Model reference

The subsystem-level *why* behind the model: what each design decision encodes, and which ARD argues it. Three layers sit around this doc —

| Layer | Where | Answers |
|---|---|---|
| Mechanism | `docs/odd-protocol.md` | What the code computes, formula by formula |
| **Rationale digest** | **this file** | Why each subsystem works that way, with ARD refs |
| Full argument | `docs/decisions/NNN-*.md` | What was considered and rejected |

Read this when changing a subsystem. Read the ARD when the digest doesn't say enough.

## Design decisions by subsystem

- **Person stats are mutable, collections are not reassignable**: primitive fields (`age`, `resources`, etc.) are mutable so events can update them in place. Collection fields (`killed`, `hasChildren`, `childOf`, etc.) are `readonly` to prevent accidental replacement — but their contents remain mutable (e.g. `seed()` pushes parents into `childOf` post-construction). Constructor accepts 0, 1, or 2 parents; 1-parent entries arise only from `Simulation.seed()` for single-parent households. See ARD 002, ARD 052.
- **Object references as identity**: `Person` objects have no ID field. Reference equality (`===`) is identity. See ARD 001.
- **Stats and intents start at 0** in the constructor, but `Simulation.seed(n, rng)` randomizes them on startup. Age is a young-skewed power taper over [`SEED_AGE_FLOOR`,`SEED_AGE_MAX`) (`SEED_AGE_DISTRIBUTION_EXPONENT` sets steepness) so elders exist and senescence engages at tick 0 (ARD 056). Resources are 0 for children (`age < WORKING_AGE_MIN`, parentally subsidized) and a compressed band `SEED_ADULT_RESOURCES_MEAN·(1 ± SEED_ADULT_RESOURCES_SPREAD)` for adults, so starting adult Gini reflects the model not the seed shape (ARD 057). Experience is [0, min(age, EXPERIENCE_CAP, effective-accumulated-years)] (childhood years count at `EXPERIENCE_CHILDHOOD_FACTOR`, so children aren't over-experienced). Other fields: intelligence/constitution/charisma [1,10], learningIntent/exerciseIntent [0,1), stealingIntent [0,0.3), killingIntent [0,0.1), helpingIntent [0,0.5). Education forms a coherent ladder (a credential-in-progress implies the prior tier is complete): ages ≤17 → `isWorkingOnEd = HIGH_SCHOOL` at `GRADUATION_HS_SEED_RATE`; ages 18–24 → completed `education = HIGH_SCHOOL` at `GRADUATION_ADULT_HS_RATE`, then `isWorkingOnEd = BACHELORS` at `GRADUATION_COLLEGE_SEED_RATE` (so a college student always holds HS); ages 25+ → completed `education` seeded hierarchically (HS, then BACHELORS/MASTERS/PHD conditional). After person creation, `seed()` runs three passes: (1) **parent assignment** — each child (age < `RELATIONSHIP_MIN_AGE`) is assigned to a family unit (two-parent at `SEED_TWO_PARENT_FRACTION` picking age-proximate co-parents, single-parent otherwise; siblings share families at `SEED_SIBLING_REUSE_PROBABILITY`; parent must be at least `SEED_MIN_PARENT_AGE_GAP` years older; unmatched children remain orphaned); two-parent families set `isInRelationshipWith` on both parents. (2) **adult pairing** — remaining unpartnered adults are sorted by age and paired with their nearest-age neighbour until the adult paired fraction reaches `SEED_PAIRING_FRACTION` (consistent with ARD 054's age-gap preference). (3) **employment** — working-age persons are seeded employed toward `SEED_EMPLOYMENT_RATE`, ranked by a JobEvent-style employability score (ARD 058), so tick 0 isn't an artificial 0% employment crater. Extraction order is shuffled each tick via Fisher-Yates using the seeded RNG. See ARD 021, ARD 052, ARD 053, ARD 054, ARD 056, ARD 057, ARD 058.
- **`happiness` is a computed getter** (not a stored stat). Factors: job (+5 if employed; −3 if unemployed and working-age 18–65 only), resources (critical/low/comfortable thresholds vary by age group), relationship (+3), age (>65: −1), illness (−round(illness×5)), `helpHappinessBoost` (set by HelpEvent, decays per tick), `killHappinessBoost` (set by KillEvent on a confirmed kill, decays per tick). Children use average living parents' resources instead of their own. Floor 0. See ARD 009 (original), ARD 014 (revision), ARD 046 (transient boosts).
- **Records are plain data classes** — they record that an event happened, they don't trigger anything.
- **Global natural resource pool (conservative)**: `Simulation` owns `naturalResources` (current pool, initialized from `NATURAL_RESOURCES_INITIAL` independently of the ceiling — ARD 044), `naturalResourceCeiling` (max accessible, initialized from `NATURAL_RESOURCE_CEILING_INITIAL`), and `extractionProductivity` (multiplier on gather output and pool drain; starts at `EXTRACTION_PRODUCTIVITY_INITIAL = 1.0`, bounded to `[EXTRACTION_PRODUCTIVITY_FLOOR, MAX_EXTRACTION_PRODUCTIVITY]` — ARD 047; higher = more output AND faster pool drain). Pool regenerates by `naturalResourceCeiling × NATURAL_RESOURCE_REGEN_FRACTION` each tick (clamped at ceiling) via `simulation.regenerate()`, called at the start of each tick in `LooperSingleton` — coupling means ceiling-growth inventions actually unlock new sustainable population (ARD 043). The **carrying capacity is dynamic and binding** (ARD 050): each tick `simulation.degradeCeiling()` (called before `regenerate()`) erodes the ceiling by `ceiling × CEILING_DEGRADATION_RATE × depletionFraction` (no loss at a full pool, max loss at an empty one), floored at `NATURAL_RESOURCE_CEILING_FLOOR`, so overexploitation degrades carrying capacity (HANDY/Tainter); `MAX_NATURAL_RESOURCE_CEILING` is a small multiple of the initial ceiling so regen stays comparable to extraction and the commons binds. `GatherResourcesEvent` and `WindfallEvent` are both strict-conservation pool draws (drain == personal gain); when the pool is empty they fire as no-ops. `InventionEvent` randomly shifts productivity (tech-boom/austerity) or ceiling; ceiling-growth is the most frequent outcome (weights 1:1:2) but uses a gentle dedicated `INVENTION_CEILING_GROWTH_SCALAR` (ARD 050) so it doesn't ratchet to the cap; productivity is a bounded symmetric random walk and ceiling growth is capped at `MAX_NATURAL_RESOURCE_CEILING` (ARD 047). See ARD 007, ARD 039, ARD 040, ARD 043, ARD 044, ARD 047, ARD 050.
- **Age modifiers**: mortality uses a U-shaped curve (`ageMortalityModifier` getter on `Person`); all event probabilities are multiplied by a per-event bell curve via `ageModifier()` in `Helpers/AgeModifier.ts`. See ARD 008.
- **Experience grows and decays each tick**: `ExperienceEvent` (unconditional) computes `BASE_EXPERIENCE_GROWTH + intelligence * INTELLIGENCE_EXPERIENCE_SCALAR * learningFade ± activity modifier`, clamped to `[0, EXPERIENCE_CAP]`. Childhood attenuates growth; education and employment accelerate; adult/elderly idleness decays. Intelligence fade reuses the learning age curve. See ARD 017.
- **`illness` is live continuous severity `[0, 1]`**: `IllnessEvent` (unconditional, fires before `GatherResourcesEvent`) rolls onset (`BASE_ILLNESS_ONSET * ageRisk / constitution`) and recovery (`BASE_ILLNESS_RECOVERY * constitution / ageRisk * senescence`) independently each tick; severity clamped after both rolls. `ageRisk = 1 + age / ILLNESS_AGE_RISK_DIVISOR` (linear). `senescence = max(FLOOR, 1 - DECAY * max(0, age - START_AGE))` decays recovery with age so chronic illness accumulates in the old (ARD 049). See ARD 018, ARD 049.
- **Mortality is disease-mediated, not suicide-dominated** (ARD 049): suicide (`SUICIDE_PROBABILITY_SCALE / (happiness+1)`) was cut ~2 orders of magnitude to realistic rates (~1–4% of deaths); illness senescence + re-tuned onset/recovery/`ILLNESS_DEATH_SCALAR` make age-modified illness death (`illness × ILLNESS_DEATH_SCALAR × ageMortalityModifier`) the dominant old-age cause. No separate "natural"/"old age" cause — age death routes through `CAUSE_OF_DEATH.ILLNESS`. *Follow-up resolved: realistic mortality exposed a fertility/population-regulation problem; ARD 050 bound the carrying capacity and a sweep-harness study (`docs/research-fertility.md`) showed no `BASE_CHILDBIRTH_RATE` stabilizes the population — so boom-bust is accepted as a HANDY behavior and the rate is set to 0.6 for outcome variety. A follow-up tuning study (`docs/research-tuning-defaults.md`) generalized this: the model is a terminal one-shot overshoot, *no* single constant yields a sane long-run default, and short-horizon variety is a mid-overshoot artifact — tune for short-horizon variety, judge at long horizons, and treat long-run stability as needing a structural fix (crash recovery), not recalibration.* See ARD 049.
- **Resource consumption**: `ConsumptionEvent` (unconditional, after `GatherResourcesEvent`) deducts age-scaled living costs each tick. Children with living parents pay a small % of own resources (starvation cannot fire at zero — implicit parental subsidy); orphaned children and adults pay a flat rate scaled by `CONSUMPTION_ELDER_MULTIPLIER` at 65+. Resources floor at 0; when they hit 0 and cost > 0, `STARVATION_ILLNESS_RATE` is added to illness, feeding the existing illness→mortality chain in `MisfortuneEvent`. No separate starvation death path. See ARD 024.
- **`isInRelationshipWith` lifecycle managed by `RelationshipEvent` + `kill()`**: persons below `RELATIONSHIP_MIN_AGE` skip `RelationshipEvent` entirely (ARD 053); formation requires both persons unpartnered; dissolution uses a flat per-tick rate (`BASE_BREAKUP_RATE`); partner death clears the surviving partner's field, dropping the +3 happiness bonus as a widowhood proxy feeding MisfortuneEvent's suicide check. `BASE_RELATIONSHIP_RATE` raised to improve post-crash re-pairing at low population (ARD 053). Resource pooling deferred. See ARD 025, ARD 053.
- **10-year summary and progress reporting**: Every 10 ticks, `LooperSingleton` builds a `TenYearSummary` (averaged Gini/resources/happiness/naturalResources/naturalResourceCeiling/orphan count/child population/welfare recipients/population, peak Gini, delta death counts by cause, population delta), appends it to `Simulation.decadeHistory`, and prints a one-line console summary. `TenYearSummary` is defined in `Types.ts`. Formatting lives in `src/Helpers/Reporters.ts` (pure functions). See ARD 015.
- **End-of-simulation report**: After the tick loop, `index.ts` calls `formatEndReport` (console summary with outcome verdict) and `writeReportHTML` (writes `output/report-<seed>-<timestamp>.html` — self-contained HTML with Chart.js charts loaded from CDN). Outcome classification (`COLLAPSE`/`STRUGGLING`/`CYCLICAL`/`STABLE`, plus `EXTINCTION`) uses named threshold constants in `Variables.ts` and a `CycleMetrics` the caller supplies (ARD 063). I/O in `src/Helpers/ReportWriter.ts`, pure formatting in `Reporters.ts`. See ARD 016.
- **Inequality is measured over adults** (ARD 060): `resourceGini(persons)` in `Helpers/Inequality.ts` computes the Gini of `resources` over persons at or above `WORKING_AGE_MIN`, and is the single definition used by both `Simulation.snapshot()` and `KillEvent`'s inequality→violence multiplier (the two previously duplicated the helper and could have drifted). Dependent children are excluded because the model does not treat a child's own `resources` as their standard of living anywhere else — `ConsumptionEvent` charges them a token rate assuming a parental subsidy (ARD 024), `happiness` substitutes the parents' average (ARD 014), and both seeding paths start them at zero (ARD 037, ARD 057) — so including them tied the primary collapse signal to the dependency ratio, and hence to the birth rate. Returns 0 when no adults are alive. ARD 051's three Gini thresholds were re-derived on this basis by quantile preservation; the two lower ones moved, COLLAPSE stood because the distributions' upper tails coincide. Measurement in `docs/research-thriving-reachability.md`. See ARD 060.
- **Multi-dimensional outcome classification** (ARD 051, revised by ARD 063): `classifyOutcome(decadeHistory, startPopulation, cycles)` reads population trajectory (Tainter/Turchin/Diamond, not HANDY-specific), inequality (final-decade Gini), wellbeing (happiness), and ecological strain (commons fill = final-decade `avgNaturalResources ÷ avgNaturalResourceCeiling`), against a `CycleMetrics` (`CycleDetector.detectCycles` over the run's population series) the caller computes once and reuses for both the sweep harness's `cyc`/`stable` columns and this call. COLLAPSE fires on extreme Gini unconditionally, or on severe peak-relative decline when the population is *not* a confirmed stable cycle (`cycles.stableCycle`, gated by `CYCLICAL_MIN_CYCLES`/`CYCLICAL_TROUGH_HOLD_FRACTION`); STRUGGLING on any single stress signal — Gini, happiness, commons, or (only when not cycling) peak-decline; CYCLICAL when a confirmed stable cycle clears every stress signal; STABLE otherwise; EXTINCTION (ARD 031) still checked first. `explainOutcome` names the driving dimension, citing cycle count and trough trend for CYCLICAL. ARD 063 replaced THRIVING: `docs/research-scale-robustness.md` found the model's first non-extinct regime and showed the old peak-relative-decline check mislabeled it COLLAPSE or STRUGGLING depending on where in its cycle a run was measured, and separately showed THRIVING's near-peak-population-*and*-healthy-commons requirement is one that regime is structurally barred from — population peak is exactly what empties the commons under cycling. Refines ARD 016 and ARD 051; research in `docs/research-collapse-classification.md` and `docs/research-scale-robustness.md`. `docs/research-thriving-reachability.md` is historical — it measured against the now-retired THRIVING label; the underlying blockers it found (`extractionProductivity`'s log-asymmetric drift, the commons having no feedback before exhaustion) remain live issues, tracked in `docs/future-ideas.md`. See ARD 063.
- **Character types** (config-driven, predicate-based): `simulation.personTypes` in the config maps a name to `{ percentage, ranges }`; ranges are partial `[min, max)` overrides on the 11 numeric fields (`age`, `resources`, `experience`, `intelligence`, `constitution`, `charisma`, `learningIntent`, `exerciseIntent`, `stealingIntent`, `killingIntent`, `helpingIntent`). At seed time, `floor(n * percentage)` persons are quota-allocated per type, Fisher-Yates shuffled, then seeded with the overrides; undeclared fields fall back to defaults. `education`/`isWorkingOnEd` stay age-keyed. No `Person.type` field — a `classifyPerson` predicate evaluated against current stats produces the per-type counts in the end report's `COHORT SURVIVAL` section, so "did engineers grow or shrink?" is answered from current stats, not seed labels. Catalog of proposed archetypes in `docs/research-character-types.md`. See ARD 030.
- **Per-decade observer hook**: `LooperSingleton.start()` takes an optional sixth argument `onDecade(simulation, endTick)`, called at each decade boundary after the `TenYearSummary` is recorded (and once more for the partial decade at run end). It exists because a probe that wants per-person state *over time* could otherwise only sample `getLiving()` once, at run end — which is why `gini-decomp.ts` re-runs the whole simulation per time point, an approach that does not scale to one sample per decade. Observation only: it must not mutate the simulation, and with the argument omitted the tick loop is unchanged. Verified bitwise-identical to the pre-hook engine with `scripts/parity-check.ts` (8 seeds × 200 ticks).

- **Survivor composition, EXTINCTION, and partial-decade summary**: `formatEndReport` renders a `SURVIVORS` section (age/education/employment/health/family buckets) whenever any persons live. `classifyOutcome` adds `EXTINCTION` (checked first, fires when `endPopulation === 0`); the report appends `Extinct as of Yr NNN` when that fires and the HTML report uses a distinct darker-red color. `LooperSingleton` appends one partial-decade `TenYearSummary` to `decadeHistory` after the loop when `ticks % 10 !== 0`, so the final-decade metrics reflect the actual end of the run instead of the last full decade. See ARD 031.
- **Pool dynamics and invention counters in snapshots**: `TickSnapshot` captures `extractionProductivity` and `naturalResourceCeiling` each tick; `Simulation` keeps cumulative `inventionFasterCount`/`inventionSlowerCount`/`inventionCeilingCount` incremented by `InventionEvent`. Surfaced as a one-line "Inventions:" entry in `RESOURCES` and a "Resource Pool Dynamics" chart in the HTML report. See ARD 032.
- **Birth tracking symmetric with deaths**: `Simulation.recordBirth()` increments `tickBirths` (flushed on snapshot to per-tick `births` and `cumulativeBirths` fields, same lifecycle as `tickDeathCauses`); `ChildbirthEvent` calls it after `simulation.add(child)` (the seed loop does not). `TenYearSummary` gains `births`, derived from cumulative deltas; reports show Births in `POPULATION`, the decade summary line, the decade table, and the HTML population chart. See ARD 033.
- **Community pool, taxation, and welfare**: `Simulation` owns a `communityPool` field. Each tick a flat `TAX_RATE` fraction is deducted from every living person's resources and added to the pool; jail forfeitures (ARD 035) are a second funding source. After consumption, `distributeWelfare` tops recipients up **toward** `WELFARE_THRESHOLD` out of `communityPool * (1 - COMMUNITY_POOL_RESERVE_FRACTION)` (ARD 061, revising ARD 034's equal split): a recipient is anyone short of the threshold **except parentally subsidised children** (`age < CONSUMPTION_CHILD_MAX_AGE` with a living parent — ARD 062; their need is met by topping up their parents, and orphans remain recipients at any age), nobody receives more than their own shortfall, and when the distributable amount covers every shortfall the surplus **stays in the pool** as a buffer; when it doesn't, the distributable amount is split in proportion to shortfall. Consequences: welfare can no longer lift anyone above the threshold (the old equal split could make a handful of people rich when few qualified — Gini 0.17 → 0.86 in one observed run), the pool now accumulates in prosperous periods instead of being drained to its reserve every tick, and `COMMUNITY_POOL_RESERVE_FRACTION` now caps crisis drawdown rather than creating the buffer. ARD 061 dropped ARD 034's orphan clause as inert under a top-up; ARD 062 makes the living-parent test load-bearing again from the other direction. ARD 062 exists because ARD 060 and ARD 061 contradicted each other: 060 removed children from the inequality signal on the grounds that a child's own `resources` is not their welfare, and 061 then sized their payment from exactly that field, sending ~46% of all welfare to under-18s where it cannot reach their consumption, happiness or the Gini — inert until they turn 18, while rationing bound during crashes. The exclusion reuses `ConsumptionEvent`'s subsidy boundary (15) rather than `WORKING_AGE_MIN`, because 15–17-year-olds with living parents pay the flat adult rate and can starve. See ARD 034, ARD 061, ARD 062.
- **Jail and retribution**: `Person` gains `jailedTicksRemaining` (default 0). Detection is checked inside `StealEvent` and `KillEvent` after a crime; probability scales with cumulative prior crimes and crime severity. On detection, most resources are forfeited to `communityPool` and `jailedTicksRemaining` is set to a fixed sentence per crime type. `LooperSingleton` decrements the counter each tick before `EventFactory`. While jailed, only `AgeEvent`, `IllnessEvent`, and `JailEvent` run; `JailEvent`'s flat gather is now drawn from `communityPool` (clamped to what the pool can supply — empty pool = starvation), consumption is unchanged. See ARD 035, ARD 041.
- **Dynamic intent multipliers and theft emboldening**: `stealingIntent` can now increase permanently — each undetected theft bumps it by `STEALING_EMBOLDEN_INCREMENT`, capped at `STEALING_INTENT_CAP` (social learning / criminal career escalation). Situational pressure adds transient in-event multipliers without touching stored fields: low resources amplify steal probability in the `StealEvent` gate; low happiness amplifies kill attempt probability in `KillEvent`. See ARD 036.
- **Newborn trait seeding via parental heritability**: `ChildbirthEvent` draws a newborn's three stats and four intents through one function (ARD 064, superseding ARD 037): the trait's current mean across the living population, plus the parental midpoint's deviation from it scaled by a heritability coefficient (separate ones for stats and intents), plus a Gaussian residual scaled by that trait's own live standard deviation. Intents clamp to [0, 1]; stats are unclamped. `Simulation.traitDistribution` supplies the mean and spread, cached per tick so every child born in a tick regresses toward the same population state. Below `HERITABILITY_MIN_SAMPLE` living people the draw anchors to the parents and takes its spread from the founder seeding range in `TraitRanges.ts`. `helpingIntent` is **not** inherited — a known defect with its own ARD pending. The three fields a life can change — `intelligence`, `constitution`, `stealingIntent` — each carry a companion endowment field that is set at birth or seeding and never changed; heritability reads the endowment, the rest of the model reads the expressed value, and a newborn expresses exactly its endowment (ARD 066). Without that split, regressing toward the expressed mean ratchets those traits to their caps — measured, with the before/after, in `docs/research-heritability-anchor.md`.
- **Estate inheritance split on death**: `Simulation.kill()` distributes `person.resources` across `communityPool`, surviving `isInRelationshipWith`, and living children (`hasChildren.filter(c => c.causeOfDeath === null)`) via three constants (`ESTATE_COMMUNITY_SHARE`, `ESTATE_PARTNER_SHARE`, `ESTATE_CHILDREN_SHARE`, sum = 1.0). Missing-heir shares consolidate to the other individual heir; with no individual heirs the full estate goes to the community pool. Cause-blind — murder follows the same rules; the killer receives no share from the estate. Closes the resource-conservation loop alongside ARDs 039–041. See ARD 042.

## Implementation surface

Event mechanisms live in `docs/odd-protocol.md` §7 (Submodels) — formulas, gates and ARD refs, kept current with the code. What follows is the surrounding API surface: class methods, snapshot fields, and reporting.

- `Person` data model — all properties, mutable primitives, readonly collections, `happiness` getter (ARD 014: job+5/−3 for working-age only, age-group resource thresholds, children use living-parents avg, floor 0; ARD 046: adds `helpHappinessBoost + killHappinessBoost`), `livingParents` getter, `ageMortalityModifier` getter (U-shaped curve, ARD 008); `jailedTicksRemaining` (countdown to freedom, default 0; decremented by LooperSingleton before EventFactory each tick, ARD 035); `helpingIntent` (seeded higher than antisocial intents, ARD 045); `helpHappinessBoost` / `killHappinessBoost` (transient, decayed per tick by LooperSingleton, ARD 046)
- `Simulation` — `living`, `deceased`, `history`, `decadeHistory`; `getLiving()`, `getDeceased()` (shallow copy of the dead, for age-at-death reporting), `indexOfLiving()`, `getRandomOther()`, `kill()` (distributes estate per ARD 042 before clearing partner/filter), `add()` — the last four are backed by a private `livingIndex` map maintained by `add`/`kill`, so position lookup and random-other selection are O(1) and allocation-free rather than scanning or copying the population, `recordBirth()` (ARD 033), `recordConsumption()` (per-tick consumption accumulator), `seed(n, rng, personTypes?)`, `snapshot()`, `regenerate()`, `degradeCeiling()` (erodes the ceiling proportional to pool depletion, floored at `NATURAL_RESOURCE_CEILING_FLOOR`, ARD 050); adult-basis Gini computed per tick via `resourceGini` (ARD 060); `naturalResources`, `naturalResourceCeiling`, `extractionProductivity` resource pool fields (ARD 007, ARD 039); cumulative `inventionFasterCount`/`inventionSlowerCount`/`inventionCeilingCount` (ARD 032); `personTypes` and `seededTypeCounts` retained for ARD-030 end-of-run reporting; `communityPool` funded by `collectTax()` each tick, jail forfeitures, and estate community-shares (ARD 042); drained by `distributeWelfare()` (tops adults and orphans up toward `WELFARE_THRESHOLD` after consumption, skipping parentally subsidised children, retaining the surplus; proportional to shortfall when the pool is short — ARD 034, ARD 061, ARD 062) and by `JailEvent` gather (ARD 041)
- `LooperSingleton.start(n, ticks, seed, logger?)` — full tick loop: prints header, seeds simulation, calls `degradeCeiling()` then `regenerate()` (ARD 050) then runs EventFactory per person per tick, calls `snapshot()` each tick; builds and stores a `TenYearSummary` every 10 ticks (ARD 015); after the loop appends a partial-decade summary when `ticks % 10 !== 0` (ARD 031); each tick: `collectTax` before EventFactory, `jailedTicksRemaining--` (floored at 0) + `helpHappinessBoost`/`killHappinessBoost` decay before EventFactory, `distributeWelfare` after EventFactory (ARD 034, ARD 035, ARD 046)
- `DeathRecord`, `KillingRecord`, `StealingRecord` data classes
- `SeededRandom` (LCG), `RNG` type, `Constants`, `Variables` (includes `HAPPINESS_BASELINE`, `PRIME_AGE`, `AGE_DEATH_CURVATURE`, `BASE_GATHER_AMOUNT`, `INTELLIGENCE_GATHER_SCALAR`, `SUICIDE_PROBABILITY_SCALE`, `ILLNESS_DEATH_SCALAR`, resource-pool constants (including ARD 050 carrying capacity: `MAX_NATURAL_RESOURCE_CEILING`, `NATURAL_RESOURCE_CEILING_FLOOR`, `CEILING_DEGRADATION_RATE`, `INVENTION_CEILING_GROWTH_SCALAR`), disaster constants, experience constants, illness constants (including ARD 049 senescence: `ILLNESS_RECOVERY_SENESCENCE_START_AGE`, `ILLNESS_RECOVERY_SENESCENCE_DECAY`, `ILLNESS_RECOVERY_SENESCENCE_FLOOR`), job constants (`JOB_GAIN_EXPERIENCE_SCALAR`, `JOB_GAIN_CHARISMA_SCALAR`, `JOB_LOSS_BASE`, `JOB_LOSS_STAT_SCALAR`, `EDUCATION_JOB_GAIN_SCALAR`), graduation constants (`BASE_GRADUATION_RATE`, `GRADUATION_HS_MAX_AGE`, `GRADUATION_COLLEGE_MAX_AGE`, `GRADUATION_HS_SEED_RATE`, `GRADUATION_COLLEGE_SEED_RATE`), enrollment constants (`BASE_ENROLLMENT_RATE`), seed-structure constants (ARD 052/056/057/058: `SEED_AGE_FLOOR`, `SEED_AGE_MAX`, `SEED_AGE_DISTRIBUTION_EXPONENT`, `SEED_ADULT_RESOURCES_MEAN`, `SEED_ADULT_RESOURCES_SPREAD`, `SEED_EMPLOYMENT_RATE`, `SEED_EMPLOYMENT_SCORE_NOISE`, plus the ARD 052 pairing/parent constants), and per-event age profile constants for every event wired into `EventFactory`, including graduation and enrollment; `GatherResourcesEvent` has no profile because it is unconditional and age-blind (ODD §7). `Variables.validate()` throws on a violated cross-constant invariant — the ARD 042 estate shares summing to 1.0, and (ARD 067) the adult happiness ladder, elderly happiness ladder, and childbirth resource ramp each being strictly ordered — and is called on every `--set`/`--config` override path. ARD 067 also documents eleven resource-threshold constants' implied years of consumption at `CONSUMPTION_BASE`, as comments only: `HAPPINESS_RESOURCE_*_THRESHOLD[_ELDERLY]`, `WELFARE_THRESHOLD`, `CHILDBIRTH_RESOURCE_MIN`/`CHILDBIRTH_RESOURCE_SCALE`/`CHILDBIRTH_BIRTH_COST`, `SEED_ADULT_RESOURCES_MEAN` — `WELFARE_THRESHOLD`, `CHILDBIRTH_BIRTH_COST`, and `SEED_ADULT_RESOURCES_MEAN` are deliberately left unconstrained by `validate()`)
- `AgeModifier.ts` — `ageModifier(age, peakAge, scale, floor)` bell-curve helper (ARD 008)
- `Inequality.ts` — `gini(values)` (sorted weighted-sum formula, 0 for empty/equal/all-zero input) and `resourceGini(persons)` (filters to `age >= WORKING_AGE_MIN`, then `gini`). Pure; no I/O. The one inequality definition in the model — used by `Simulation.snapshot()` and `KillEvent`. See ARD 060.
- `TickSnapshot` observability: population, per-tick and cumulative death counts by cause (murder/illness/disaster/suicide/old age), `averageResources`, `resourceGini` (adult basis, ARD 060), `averageHappiness`, `aggregateKillingIntent`, `aggregateStealingIntent`, `naturalResources`, `extractionProductivity` and `naturalResourceCeiling` (ARD 032), per-tick and cumulative `births` (ARD 033), `communityPool`, `averageIllness`, `employmentRate`, `stealsCommitted`, `jailedPopulation`, `totalCoupleCount`/`fertileCoupleCount`, and (reporting-only) `averageAge`/`medianAge`, `totalConsumption` (resources consumed by the living population this tick, via `recordConsumption()` from ConsumptionEvent + JailEvent), `educationCounts` (living-population count per education tier, length 6), `childPopulation`/`orphanCount` (living children under `WORKING_AGE_MIN`, and those among them with no living parent — the same test ARD 034 welfare uses, so the orphan series and welfare eligibility always agree), and `welfareRecipients` (persons eligible at this tick's `distributeWelfare()` call, recorded at distribution time because the payout itself lifts recipients back over `WELFARE_THRESHOLD`; an empty pool still counts its eligible persons)
- `Reporters.ts` — `buildTenYearSummary(window, endTick, startPopulation)` (accepts partial trailing windows per ARD 031), `formatDecadeSummary`, `formatSimulationHeader`, `formatEndReport(..., personTypes?, seededTypeCounts?, living?, extinctionTick?, extractionProductivity?, inventionCounts?)` (renders `COHORT SURVIVAL` when types supplied; `SURVIVORS` when any persons live; `Reason:` line under OUTCOME; `Extinct as of Yr NNN` callout on EXTINCTION; `Inventions:` line in RESOURCES; `Births` in POPULATION + decade table), `classifyOutcome(decadeHistory, startPopulation, cycles)` (population trajectory read against `CycleMetrics`, Gini, happiness, commons fill; EXTINCTION at population=0, checked first — ARD 051, revised ARD 063), `explainOutcome(decadeHistory, startPopulation, outcome, cycles)` (names the driving dimension), `summarizeSurvivors` (includes an `orphans` count), `formatSurvivorSection`, `share(part, whole)` (0 when the cohort is empty). Orphans appear as a trend line in `POPULATION` (decade averages plus % of children), an `Orphans` column in the decade table, a field on the decade console line, and the `SURVIVORS` family line; welfare recipients as an `On welfare` trend line in `RESOURCES` (decade averages plus % of population) and a `Welfare` column in the decade table. All pure; no I/O. See ARD 015, ARD 016, ARD 030, ARD 031, ARD 032, ARD 033, ARD 051.
- `Classifier.ts` — `classifyPerson(person, types)`, `countPerType(persons, types)`, `parsePersonTypes(raw)`. Pure predicate-based classification + config parsing/validation (sum ≤ 1.0 enforced; malformed ranges and unknown fields warned and skipped). See ARD 030.
- `ReportWriter.ts` — `writeReportHTML(simulation, n, ticks, seed)`: writes `output/report-<seed>-<outcome>-<timestamp>.html` by compiling `report-template.hbs` with Handlebars and embedding JSON data + Chart.js charts: inequality-vs-population (Gini overlaid with population — the project's core thesis view), population & mortality (population + decade death rate per 100), happiness, health & employment, age structure (median + average age), carrying-capacity & productivity (commons-fill ratio + extraction productivity), education distribution (stacked bar of tiers over time), resource-pool dynamics (incl. population consumption per tick on its own axis), couples & fertility (incl. births), orphans over time (orphan count on the left axis, orphan share of children on the right), welfare coverage over time (recipients per tick + share of population), antisocial intent per capita, crime & punishment (thefts + jailed population), deaths by cause (area↔bar toggle), age-at-death distribution (whole-run histogram by age bucket, stacked by cause — built from `simulation.getDeceased()`), and wealth-by-age (end-of-run living population: avg resources per age bucket as bars + headcount overlay — built from `simulation.getLiving()`). EXTINCTION outcome color added. Murders are shown only once (deaths-by-cause), not duplicated in the crime chart. See ARD 016, ARD 031, ARD 032, ARD 033, ARD 038.
- `index.ts` — after the run, prints `formatEndReport` to console and calls `writeReportHTML`; `output/` is gitignored
- `Variables.ts` — outcome classification thresholds (ARD 051, revised ARD 063): `COLLAPSE_GINI_THRESHOLD`, `COLLAPSE_PEAK_DECLINE_FRACTION`, `STRUGGLING_GINI_THRESHOLD`, `STRUGGLING_HAPPINESS_THRESHOLD`, `STRUGGLING_PEAK_DECLINE_FRACTION`, `STRUGGLING_RESOURCE_FRACTION`, `CYCLICAL_MIN_CYCLES`, `CYCLICAL_TROUGH_HOLD_FRACTION` (`COLLAPSE_POPULATION_FRACTION` dropped — superseded by peak-relative decline; the four `THRIVING_*` thresholds dropped with the label — ARD 063). The three Gini thresholds were re-derived for the adult basis in ARD 060 by quantile preservation; COLLAPSE stands unchanged.
- Tests for all of the above
